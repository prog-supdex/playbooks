---
  - hosts: elk_host
    gather_facts: no
    become: yes
    roles:
      - elasticsearch

  - hosts: elk_host
    become: yes
    gather_facts: no
    roles:
      - logstash

  - hosts: filebeat_host
    become: yes
    gather_facts: no
    roles:
      - filebeat

  - hosts: elk_host
    become: yes
    gather_facts: no
    roles:
      - kibana    
